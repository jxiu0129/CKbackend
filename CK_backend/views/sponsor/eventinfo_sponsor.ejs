<%-include('../root/header')-%>

<!-- Main -->
<section id="main" class="wrapper">
	<div class="inner">
		<header class="align-center">
			<h3><u><%=event.name%></u>的詳細資訊</h3>
		</header>

		<!-- Table -->
		<div class="table-wrapper">
			<table>
				<tbody>
				<tr>
					<td width="400px" style= "word-break:break-all">活動名稱</td>
					<td width="600px" style= "word-break:break-all"><%=event.name%></td>
				</tr>
				<tr>
					<td>活動開始時間</td>
					<td><%=Time%></td>
				</tr>
				<tr>
					<td>活動結束時間</td>
					<td><%=EndTime%></td>
				</tr>
				<tr>
					<td>活動地點</td>
					<td><%=event.location%></td>
				</tr>
				<tr>
					<td>投資點數</td>
					<td>
						<img src="../../images/piggy-bank.png" height="20" width="20"/>
						<%=event.expense%>
					</td>
				</tr>
				<tr>
					<td>參加人數</td>
					<td><a href="/sponsor/events/<%=event._id%>/attendancelist"><%=event.amount%></td>
				</tr>
				<tr>
					<td>活動狀態</td>
					<td>
						<%if(event.status == 'willhold'){%>
							尚未開始
						<%}else if( event.status == 'holding'){%>
							正在進行
						<%}else if( event.status == 'finish'){%>
							活動已結束
						<%}%>
					</td>
				</tr>
				<tr>
					<td>發點數條件：</td>
					<td>
						<%if(event.signCondition == 'onlyIn'){%>
							只要簽到
						<%}else if(event.signCondition == 'onlyOut'){%>
							只要刷退
						<%}else if(event.signCondition == 'bothSign'){%>
							簽到與刷退
						<%}%>
					</td>
				</tr>
				<tr>
					<td>活動報名連結</td>
					<%if(event.ncculink) {%>
						<td><a href="<%=event.ncculink%>" target="_blank">報名連結</td>
					<%}else{%>
						<td>無報名連結</td>
					<%}%>
				</tr>
			</tbody>
		</table>
	</div>

		<!--button-->
		<ul class="actions fit">
			<li><a href="/sponsor/events" class="button special fit">回到上一頁</a></li>
			<li><a href="" class="button fit" data-toggle="modal" data-target="#end">
				<img src="../../images/stop.png" height="15" width="15"/>活動結束</a>
			</li>
		</ul>

		<!-- Modal -->
		<div class="modal fade" id="end" tabindex="-1" role="dialog" aria-labelledby="end_event" aria-hidden="true">
			<div class="modal-dialog" role="document">
				<div class="modal-content">
					<div class="modal-header">
						<h3 class="modal-title" id="event-deleteLabel">活動結束</h3>
						<button type="button" class="close" data-dismiss="modal" aria-label="Close">
							<span aria-hidden="true">&times;</span>
						</button>
					</div>
					<div class="modal-body">
						<p>請注意！當你按下活動結束，該活動將開始發放點數。</p>
						<p>一旦開始發放點數，活動細則及出席表將再也無法更動。</p>
					</div>
					<div class="modal-footer">
						<button type="button" class="btn btn-secondary" data-dismiss="modal">取消</button>
						<button type="button" onclick="location.href='/sponsor/events/<%= event._id %>/SendMultiPoint'" class="btn btn-primary">活動結束</button>


					</div>
				</div>
			</div>
		</div>
	</div>
</section>


<%-include('../root/footer')-%>